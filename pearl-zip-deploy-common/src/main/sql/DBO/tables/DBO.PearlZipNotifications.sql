/*
 * Copyright © 2022 92AK
 */

-- DECLARATION
CREATE TABLE DBO.PearlZipNotifications(
      id INT GENERATED BY DEFAULT AS IDENTITY,
      topicId INT NOT NULL REFERENCES DBO.Topic(id),
      message VARCHAR(2000) NOT NULL,
      creationTimestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
      PRIMARY KEY(id)
);

-- PERMISSIONS
GRANT SELECT ON DBO.PEARLZIPNOTIFICATIONS TO PUB;
